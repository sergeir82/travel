GEMINI_API_KEY=your_key_here
# Optional override. If unset, the server auto-picks a working model for your API key.
# Example values vary over time; set only if you know an available model for your key.
# GEMINI_MODEL=gemini-1.5-flash

# Public key for Yandex Maps JS API (shown in the browser).
NEXT_PUBLIC_YANDEX_MAPS_API_KEY=your_key_here

